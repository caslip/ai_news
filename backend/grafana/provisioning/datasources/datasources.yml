# Grafana provisioning datasources for Loki

apiVersion: 1

datasources:
  - name: Loki
    type: loki
    access: proxy
    url: http://localhost:3100
    isDefault: true
    editable: false
    jsonData:
      timeout: 60
      httpHeaderName1: ""
